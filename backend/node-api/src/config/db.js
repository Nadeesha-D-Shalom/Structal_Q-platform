require("dotenv").config();

const config = {
  DB_USER:sa,
DB_PASSWORD:1234,
DB_SERVER:localhost,
DB_NAME:StructalQ,
  options: {
    encrypt: false,
    trustServerCertificate: true
  }
};

module.exports = config;