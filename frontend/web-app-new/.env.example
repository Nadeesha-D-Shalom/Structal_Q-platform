# Optional: absolute API origin for production or when not using CRA dev proxy.
# Example: http://localhost:5000
# Leave unset during `npm start` to use relative /api/... (proxy to backend).
# REACT_APP_API_URL=
