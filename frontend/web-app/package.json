{
  "name": "frontend-web-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "npm --prefix my-app start",
    "build": "npm --prefix my-app build",
    "test": "npm --prefix my-app test",
    "install:app": "npm --prefix my-app install"
  }
}